import React from "react";

const Dashboard = () => {
    return (
        <div className="dashboard">
            <h1 className="page_heading">
                <span>Dashboard</span>
            </h1>
        </div>
    );
};

export default Dashboard;
