import React from "react";

const Upload = () => {
    return (
        <div className="upload">
            <h1 className="page_heading">
                <span>Upload</span>
            </h1>
        </div>
    );
};

export default Upload;
